# RabbitMQ restricts the default `guest` user to localhost connections by
# default. The worker connects as `guest`/`guest` (see .env.example) over the
# `internal` Docker network, not localhost, so that restriction must be lifted
# for this local/demo deployment.
loopback_users.guest = false

log.console.level = info
